#include  <stdio.h>

int main () {
	int cont, j, k, n, numero;
 	
	printf ("Insira um numero:\n");
 	scanf ("%d", &numero);
 	printf("\n");
 	n = 0;
 	for (cont=1; cont<=numero; cont++){
  		for (j=numero; j<=cont; j++)
		printf ("");
  		for (k=1; k<=i; k++) {
  			if (n < numero) {
  				n++;
				printf ("%d ", n);
			}
		}
  		printf ("\n");
 	}
	getchar ();
	return 0;
}
